method maze()
{
  while(true){
   while(!isNet(AHEAD)){
     hop();
 }
   while(isNet(AHEAD)){
     turn(RIGHT);
    }
     if(isNet(LEFT)){
		hop();
     }
     if(isNet(RIGHT)){
		hop();
     }
    }
  
}
